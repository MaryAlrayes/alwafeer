enum HomepageNavigation {
  log,
  notification,
  home,
  delegate,
  settings,
}

enum NotificationType { pay, charge }

enum NotificationFilterType {
  all,
  pay,
  charge,
}

enum RechargePayMethod {
  visaCard,
  masterCard,
  payPal,
  contactWithDelegate
}
